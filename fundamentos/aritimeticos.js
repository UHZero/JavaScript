const [a, b, c, d] = [10, 5, 2, 8];

const soma = a + b + c + d;
const subtracao = a - b;
const multiplicacao = a * d;
const divisao = a / c;
const modulo = a % c;

console.log(soma, subtracao, multiplicacao, divisao, modulo);